## Config

`tsc --init `

`npm init -y`

`tsc -w`

`npm i lite-server`

`npm start`

## Error

```
{
  "compilerOptions": {
    "target": "ES6",
    "module": "ESNext",
    "outDir": "dist",
    "strict": true
  }
}
```